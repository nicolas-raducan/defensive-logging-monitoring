# Splunk Universal Forwarder — outputs.conf  (Win10 endpoint)
# Path: ...\SplunkUniversalForwarder\etc\system\local\outputs.conf
# Forwards collected events to the Ubuntu indexer's receiving port (9997).

[tcpout]
defaultGroup = sp

[tcpout:sp]
server = 10.0.0.100:9997
